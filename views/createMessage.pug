extends layout

block content
    .card 
      h2 Create a message 
      form(action='/log-in' method='POST')
        label(for='title') Title
        input#title(type='text' placeholder='Title' name='title' required='true' value='')
        input(type='textarea'  placeholder='...' required='true' value='')
        button(type='submit') Submit
    if (errors)
      p #{errors}